[workspace]
resolver = "2"
members = ["crates/*"]

# Centralized metadata inherited by each crate via `*.workspace = true`.
# rust-version is the declared MSRV, enforced by the `msrv` CI job.
[workspace.package]
edition = "2021"
rust-version = "1.82"
